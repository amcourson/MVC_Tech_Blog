<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label=""></span>
    <span class="create blog post">{{format_create needed_blog}}</span>
    
  </p>
  <p>
    <button class="btn btn-primary">Create Blog post</button>
  </p>
</div>
